import React from 'react';

const Title = () => {
    return (
        <header className="text-center">
            <h1 className="header">Compare text </h1>
        </header>
    );
};

export default Title;